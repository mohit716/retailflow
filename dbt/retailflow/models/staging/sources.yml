version: 2

sources:
  - name: raw
    schema: "{{ 'public' if target.name == 'dev' else 'retailflow' }}"
    database: "{{ 'airflow' if target.name == 'dev' else 'retailflow-492517' }}"
    tables:
      - name: raw_orders
      - name: raw_customers
      - name: raw_order_items
      - name: raw_payments
      - name: raw_products
